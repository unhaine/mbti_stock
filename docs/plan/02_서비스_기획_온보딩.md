# 서비스 기획 - 온보딩 플로우

> **작성일**: 2026-01-15  
> **최종 수정**: 2026-01-15  
> **상태**: 확정  
> **관련 문서**: [02_서비스_기획_랜딩페이지.md](./02_서비스_기획_랜딩페이지.md), [02_서비스_기획_메인페이지.md](./02_서비스_기획_메인페이지.md)

---

## 목차
1. [온보딩 개요](#1-온보딩-개요)
2. [사용자 시나리오별 플로우](#2-사용자-시나리오별-플로우)
3. [MBTI 선택 화면](#3-mbti-선택-화면)
4. [로딩 화면](#4-로딩-화면)
5. [데이터 저장 전략](#5-데이터-저장-전략)

---

## 1. 온보딩 개요

### 1.1 목적
- 사용자의 MBTI 유형 수집
- 부담 없는 첫 경험 제공
- 빠른 결과 확인 (3분 이내)

### 1.2 핵심 원칙
- **간단함**: 최소한의 입력 (MBTI 선택만)
- **빠름**: 로딩 3초 이내
- **재미**: 기대감을 높이는 로딩 애니메이션

---

## 2. 사용자 시나리오별 플로우

### 2.1 첫 방문자 (신규 사용자)

```
┌─────────────────────────────────────┐
│ 1. 랜딩페이지 도착                   │
│    - SNS 링크 클릭                   │
│    - 검색 유입                       │
│    - 직접 URL 입력                   │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 2. 랜딩페이지 탐색                   │
│    - Hero Section 확인               │
│    - 서비스 소개 스크롤 (선택)       │
│    - 예시 결과 미리보기 (선택)       │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 3. [시작하기] 버튼 클릭              │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 4. MBTI 선택 화면                    │
│    - 16개 버튼 표시                  │
│    - MBTI 선택 (필수)                │
│    - [다음] 버튼 클릭                │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 5. 로딩 화면 (3초)                   │
│    - 로딩 애니메이션                 │
│    - "분석 중..." 메시지             │
│    - LocalStorage에 MBTI 저장        │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 6. 메인페이지 진입                   │
│    - 가상 자산 카드 표시             │
│    - 첫 번째 테마 자동 표시          │
└─────────────────────────────────────┘
```

**예상 소요 시간**: 1-2분

---

### 2.2 재방문자 (MBTI 저장됨)

#### 시나리오 A: 랜딩페이지 재방문
```
┌─────────────────────────────────────┐
│ 1. 랜딩페이지 도착                   │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 2. LocalStorage 확인                 │
│    - MBTI 데이터 있음 감지           │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 3. [시작하기] 버튼 클릭              │
│    또는                              │
│    자동 리다이렉트 (선택)            │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 4. 메인페이지 바로 진입              │
│    - 저장된 MBTI로 데이터 로드       │
└─────────────────────────────────────┘
```

**예상 소요 시간**: 5초 이내

#### 시나리오 B: 메인페이지 직접 접속 (북마크)
```
┌─────────────────────────────────────┐
│ 1. 메인페이지 URL 직접 접속          │
│    (예: /main 또는 /)                │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 2. LocalStorage 확인                 │
└─────────────────────────────────────┘
              ↓
        MBTI 있음?
       /          \
     Yes           No
      ↓             ↓
┌──────────┐  ┌──────────────────┐
│메인페이지│  │랜딩페이지로      │
│바로 표시 │  │리다이렉트        │
└──────────┘  └──────────────────┘
```

---

### 2.3 MBTI 변경 (설정에서)

```
┌─────────────────────────────────────┐
│ 1. 메인페이지 → 설정 페이지          │
│    (푸터 메뉴 [⚙️] 클릭)            │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 2. 설정 페이지                       │
│    - 현재 MBTI 표시                  │
│    - [MBTI 변경하기] 버튼            │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 3. MBTI 선택 화면 (재진입)           │
│    - 현재 MBTI 하이라이트 표시       │
│    - 다른 MBTI 선택 가능             │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 4. 로딩 화면 (3초)                   │
│    - "새로운 투자 성향 분석 중..."   │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│ 5. 메인페이지로 복귀                 │
│    - 새로운 MBTI 데이터로 갱신       │
│    - 가상 자산 초기화 (선택)         │
└─────────────────────────────────────┘
```

---

## 3. MBTI 선택 화면

### 3.1 레이아웃

```
┌─────────────────────────────────────┐
│                                     │
│   🎭 당신의 MBTI는 무엇인가요?       │
│                                     │
│   ┌────┬────┬────┬────┐            │
│   │INTJ│INTP│ENTJ│ENTP│            │
│   ├────┼────┼────┼────┤            │
│   │INFJ│INFP│ENFJ│ENFP│            │
│   ├────┼────┼────┼────┤            │
│   │ISTJ│ISFJ│ESTJ│ESFJ│            │
│   ├────┼────┼────┼────┤            │
│   │ISTP│ISFP│ESTP│ESFP│            │
│   └────┴────┴────┴────┘            │
│                                     │
│   내 MBTI 모르겠어요 →               │
│                                     │
│          [다음]                      │
│                                     │
└─────────────────────────────────────┘
```

### 3.2 구성 요소

#### 1. 제목
```
🎭 당신의 MBTI는 무엇인가요?
```
- **폰트 크기**: 28px (모바일 24px)
- **굵기**: Bold
- **색상**: Gray-900
- **정렬**: 중앙

#### 2. MBTI 버튼 그리드 (4×4)
```
16개 버튼 배치:
분석가형 (NT): INTJ, INTP, ENTJ, ENTP
외교관형 (NF): INFJ, INFP, ENFJ, ENFP
관리자형 (SJ): ISTJ, ISFJ, ESTJ, ESFJ
탐험가형 (SP): ISTP, ISFP, ESTP, ESFP
```

**버튼 스타일:**
- **크기**: 80px × 80px (모바일 70px × 70px)
- **기본 상태**:
  - 배경: White
  - 테두리: 2px solid Gray-300
  - 텍스트: 16px, Bold, Gray-700
- **호버 상태**:
  - 배경: Primary-50
  - 테두리: 2px solid Primary-400
  - 툴팁 표시: "전략가", "논리술사" 등
- **선택 상태**:
  - 배경: Primary-500
  - 테두리: 2px solid Primary-600
  - 텍스트: White
  - 체크 아이콘 표시 (✓)

#### 3. "내 MBTI 모르겠어요" 링크
```
내 MBTI 모르겠어요 →
```
- **기능**: 16Personalities 외부 링크
- **URL**: https://www.16personalities.com/ko
- **스타일**: 
  - 폰트 크기: 14px
  - 색상: Primary-600
  - 밑줄: 호버 시만 표시
- **새 탭**: target="_blank"

#### 4. [다음] 버튼
```
[다음]
```
- **크기**: 200px × 50px (전체 너비 90% on 모바일)
- **기본 상태** (MBTI 미선택):
  - 배경: Gray-300
  - 텍스트: Gray-500
  - 비활성화 (disabled)
- **활성 상태** (MBTI 선택됨):
  - 배경: Primary-500
  - 텍스트: White
  - 호버: Primary-600
  - 클릭 가능

### 3.3 인터랙션

#### 버튼 클릭 플로우
```javascript
// 1. MBTI 버튼 클릭
onClick(mbti) {
  setSelectedMBTI(mbti);
  enableNextButton();
  showTooltip(mbti); // "전략가" 등
}

// 2. 다음 버튼 클릭
onNext() {
  if (!selectedMBTI) return;
  
  // 로딩 화면으로 이동
  navigate('/loading');
}
```

#### 애니메이션
```css
/* 버튼 클릭 시 */
.mbti-button:active {
  transform: scale(0.95);
}

/* 선택 상태 전환 */
.mbti-button.selected {
  animation: selectPulse 0.3s ease-out;
}

@keyframes selectPulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.1); }
  100% { transform: scale(1); }
}
```

---

## 4. 로딩 화면

### 4.1 레이아웃

```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│         [로딩 애니메이션]             │
│                                     │
│   당신의 투자 성향을 분석하고 있어요...│
│                                     │
│         ████████░░ 80%              │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

### 4.2 구성 요소

#### 1. 로딩 애니메이션
**옵션 A: 스피너**
```css
.spinner {
  width: 60px;
  height: 60px;
  border: 4px solid #e5e7eb;
  border-top: 4px solid #667eea;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
```

**옵션 B: 프로그레스 바**
```css
.progress-bar {
  width: 80%;
  height: 8px;
  background: #e5e7eb;
  border-radius: 4px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea, #764ba2);
  animation: progress 3s ease-out;
}

@keyframes progress {
  0% { width: 0%; }
  100% { width: 100%; }
}
```

#### 2. 로딩 메시지
```
당신의 투자 성향을 분석하고 있어요...
```
- **폰트 크기**: 18px
- **색상**: Gray-700
- **애니메이션**: 점 3개 깜빡임 (...)

**변형 메시지 (랜덤 표시):**
1. "당신의 투자 성향을 분석하고 있어요..."
2. "MBTI 기반 포트폴리오를 구성하고 있어요..."
3. "맞춤형 종목을 찾고 있어요..."
4. "투자 캐릭터를 생성하고 있어요..."

#### 3. 진행률 표시 (선택)
```
80%
```
- **폰트 크기**: 14px
- **색상**: Primary-600
- **위치**: 프로그레스 바 하단

### 4.3 로딩 시간 및 로직

```javascript
// 로딩 화면 진입
function showLoading(mbti) {
  // 1. LocalStorage에 MBTI 저장
  localStorage.setItem('userMBTI', mbti);
  localStorage.setItem('onboardingCompleted', 'true');
  localStorage.setItem('lastVisit', new Date().toISOString());
  
  // 2. 가상 자산 초기화
  initializePortfolio(mbti);
  
  // 3. 3초 후 메인페이지로 이동
  setTimeout(() => {
    navigate('/main');
  }, 3000);
}

// 가상 자산 초기화
function initializePortfolio(mbti) {
  const portfolio = {
    mbti: mbti,
    initialValue: 10000000,
    currentValue: 10000000,
    change: 0,
    changePercent: 0,
    lastUpdated: new Date().toISOString()
  };
  
  localStorage.setItem('portfolio', JSON.stringify(portfolio));
}
```

---

## 5. 데이터 저장 전략

### 5.1 LocalStorage 구조

```javascript
// 저장되는 데이터
{
  // 사용자 정보
  "userMBTI": "INTJ",
  "onboardingCompleted": "true",
  "lastVisit": "2026-01-15T09:00:00Z",
  
  // 가상 포트폴리오
  "portfolio": {
    "mbti": "INTJ",
    "initialValue": 10000000,
    "currentValue": 10250000,
    "change": 250000,
    "changePercent": 2.5,
    "lastUpdated": "2026-01-15T09:00:00Z"
  },
  
  // 북마크 (Phase 2)
  "bookmarks": ["005930", "005380"],
  
  // 설정
  "settings": {
    "theme": "light",
    "notifications": false
  }
}
```

### 5.2 데이터 유효성 검사

```javascript
// 페이지 로드 시 확인
function checkOnboardingStatus() {
  const mbti = localStorage.getItem('userMBTI');
  const completed = localStorage.getItem('onboardingCompleted');
  
  if (!mbti || !completed) {
    // 온보딩 필요
    return false;
  }
  
  // MBTI 유효성 검사
  const validMBTI = [
    'INTJ', 'INTP', 'ENTJ', 'ENTP',
    'INFJ', 'INFP', 'ENFJ', 'ENFP',
    'ISTJ', 'ISFJ', 'ESTJ', 'ESFJ',
    'ISTP', 'ISFP', 'ESTP', 'ESFP'
  ];
  
  if (!validMBTI.includes(mbti)) {
    // 잘못된 데이터, 재온보딩 필요
    localStorage.clear();
    return false;
  }
  
  return true;
}
```

### 5.3 라우팅 로직

```javascript
// App.jsx 또는 Router 설정
function App() {
  const hasCompletedOnboarding = checkOnboardingStatus();
  
  return (
    <Router>
      <Routes>
        {/* 랜딩페이지 */}
        <Route path="/" element={<LandingPage />} />
        
        {/* 온보딩 */}
        <Route path="/onboarding" element={<MBTISelection />} />
        <Route path="/loading" element={<LoadingScreen />} />
        
        {/* 메인 앱 (보호된 라우트) */}
        <Route 
          path="/main" 
          element={
            hasCompletedOnboarding 
              ? <MainPage /> 
              : <Navigate to="/onboarding" />
          } 
        />
        
        {/* 설정 */}
        <Route path="/settings" element={<SettingsPage />} />
        
        {/* 기타 */}
        <Route path="/community" element={<ComingSoon />} />
        <Route path="/portfolio" element={<ComingSoon />} />
      </Routes>
    </Router>
  );
}
```

---

## 6. 에러 처리

### 6.1 LocalStorage 저장 실패

```javascript
function saveMBTI(mbti) {
  try {
    localStorage.setItem('userMBTI', mbti);
    return true;
  } catch (error) {
    // LocalStorage 용량 초과 또는 비활성화
    console.error('LocalStorage save failed:', error);
    
    // 폴백: SessionStorage 사용
    try {
      sessionStorage.setItem('userMBTI', mbti);
      alert('브라우저 설정으로 인해 다음 방문 시 MBTI를 다시 선택해야 합니다.');
      return true;
    } catch (sessionError) {
      // 완전 실패
      alert('데이터 저장에 실패했습니다. 브라우저 설정을 확인해주세요.');
      return false;
    }
  }
}
```

### 6.2 네트워크 오류 (Phase 2)

```javascript
// API 호출 실패 시 (미래)
function handleAPIError() {
  // 캐시된 데이터 사용
  const cachedData = localStorage.getItem('cachedMBTIData');
  
  if (cachedData) {
    return JSON.parse(cachedData);
  }
  
  // 기본 데이터 사용
  return getDefaultMBTIData(mbti);
}
```

---

## 7. 접근성 고려사항

### 7.1 키보드 네비게이션

```javascript
// MBTI 선택 화면
- Tab: 버튼 간 이동
- Enter/Space: 버튼 선택
- Arrow Keys: 그리드 내 이동 (선택)
```

### 7.2 스크린 리더

```html
<!-- MBTI 버튼 -->
<button 
  aria-label="INTJ, 전략가 유형 선택"
  aria-pressed={selected}
>
  INTJ
</button>

<!-- 다음 버튼 -->
<button 
  aria-label="다음 단계로 이동"
  disabled={!selectedMBTI}
>
  다음
</button>
```

---

## 📝 변경 이력

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 2026-01-15 | 1.0 | 초안 작성 - 온보딩 플로우 상세 설계 | - |
